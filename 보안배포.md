# 점수 위조 방지 — 배포

**이번 배포는 파일 6개가 다 바뀝니다.** 게임 클라이언트와 서버가 새 프로토콜로 짝을 이루기
때문에, 하나만 올리면 점수가 아예 기록되지 않습니다. **순서대로, 끝까지** 해주세요.

---

## 0. 먼저 알아 둘 것

- 기존에 브라우저에 저장돼 있던 신분증(uid)은 서명이 없어서 **서버가 새로 발급**합니다.
  이미 올라가 있던 기록은 남지만, 그 기록의 주인은 다음 판부터 새 사람으로 잡힙니다.
  깨끗하게 가려면 3단계 마지막의 "기록 초기화"를 하세요 (선택).
- **줄넘기는 패치 스크립트를 반드시 다시 돌려야 합니다.** 안 돌리면 줄넘기 점수만
  조용히 기록되지 않습니다 (옛 클라이언트가 토큰 없이 보내서 서버가 거절합니다).

---

## 1. 서버 + 탄막 게임

```bash
cd /opt/neotone
TS=$(date +%Y%m%d-%H%M%S)
cp -a index.html index.html.bak.$TS
cp -a server.js  server.js.bak.$TS

curl -fsSL -o index.html https://raw.githubusercontent.com/wooing1/neotone/1e6bddd/index.html
curl -fsSL -o server.js  https://raw.githubusercontent.com/wooing1/neotone/1e6bddd/server.js

node --check server.js && echo "문법 OK"
systemctl restart neotone
sleep 1
curl -s localhost:9000/health; echo
```

## 2. 자동차 게임

```bash
F=/var/www/games/race/index.html
cp -a $F $F.bak.$(date +%Y%m%d-%H%M%S)
curl -fsSL -o $F https://raw.githubusercontent.com/wooing1/neotone/1e6bddd/race-index.html
```

## 3. 메인화면 + 줄넘기

```bash
R=https://raw.githubusercontent.com/wooing1/neotone/1e6bddd
curl -fsSL -o /tmp/patch-menu-rank.sh     $R/patch-menu-rank.sh
curl -fsSL -o /tmp/patch-jumprope-ui.sh   $R/patch-jumprope-ui.sh
curl -fsSL -o /tmp/patch-jumprope-rank.sh $R/patch-jumprope-rank.sh

bash /tmp/patch-menu-rank.sh
bash /tmp/patch-jumprope-ui.sh
bash /tmp/patch-jumprope-rank.sh     # ★ 이걸 빠뜨리면 줄넘기 점수가 안 올라갑니다
```

`patch-jumprope-rank.sh` 는 앵커를 못 찾으면 **파일을 건드리지 않고** 한국어로 이유를 찍고
멈춥니다. `✗` 가 나오면 그 문구를 그대로 알려 주세요 — 줄넘기 실물 소스를 못 본 상태에서
만든 스크립트라 이 한 곳만 확신이 없습니다.

### (선택) 기록 초기화

신분증 체계가 바뀌었으니 순위표를 새로 시작하고 싶다면:

```bash
systemctl stop neotone
mv /var/lib/neotone/ranks.json /var/lib/neotone/ranks.json.old.$(date +%Y%m%d-%H%M%S)
systemctl start neotone
```

## 4. 확인

```bash
echo "--- 파일"
for f in /opt/neotone/index.html /opt/neotone/server.js \
         /var/www/games/race/index.html /var/www/games/index.html \
         /var/www/games/jumprope/index.html; do
  printf "%-42s %s\n" "$f" "$(wc -c < $f)"
done

echo "--- 새 프로토콜이 들어갔는가 (전부 1 이상이어야 함)"
grep -c 'rank/start'      /opt/neotone/server.js
grep -c 'rank/start'      /opt/neotone/index.html
grep -c 'rank/start'      /var/www/games/race/index.html
grep -c 'rank/start'      /var/www/games/jumprope/index.html

echo "--- 토큰 없는 제출은 거절돼야 한다"
curl -s -X POST localhost:9000/api/rank \
  -H 'Content-Type: application/json' \
  -d '{"game":"race","score":19999,"name":"치터"}'; echo

echo "--- 잘못된 주소로 서버가 죽지 않아야 한다"
curl -s -o /dev/null -w "%{http_code}\n" 'localhost:9000/api/rank?uid=%ZZ'
curl -s -o /dev/null -w "health %{http_code}\n" localhost:9000/health
```

기대값:

- 새 프로토콜 grep 이 **네 줄 모두 1 이상**
- 토큰 없는 제출 → `{"ok":false,"err":"no token"}`
- 잘못된 주소 → `400` 이 나오고 그 다음 `health 200` (예전엔 여기서 서버가 죽었습니다)

## 5. 브라우저

네 화면 모두 **세게 새로고침** (`Ctrl+Shift+R`, 맥 `Cmd+Shift+R`, 휴대폰은 탭 닫았다 열기).
세 게임을 한 판씩 하고 랭킹에 올라오면 끝입니다.

---

## 되돌리기

```bash
cd /opt/neotone && cp -a index.html.bak.$TS index.html && cp -a server.js.bak.$TS server.js
systemctl restart neotone
cp -a /var/www/games/race/index.html.bak.* /var/www/games/race/index.html
```

메인·줄넘기 패치는 각각 같은 폴더에 `index.html.bak.<날짜시각>` 을 남깁니다.

---

## 서버 설정 한 가지 (중요)

서버가 **nginx 뒤에** 있을 때만 `X-Forwarded-For` 를 믿도록 돼 있고, 지금 배포는 그 형태라
따로 하실 일이 없습니다. 혹시 나중에 9000 포트를 인터넷에 직접 여신다면, 그때는

```bash
NEOTONE_TRUST_HOPS=0
```

를 systemd 유닛의 `Environment=` 에 넣어 주세요. 안 그러면 헤더 한 줄로 IP 제한이 뚫립니다.
(지금은 서버가 "바로 앞 상대가 로컬 프록시일 때만" 헤더를 보도록 돼 있어 자동으로 안전합니다.)

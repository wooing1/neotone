# 탄막게임 기획서 (GDD) — 프로젝트 `game_test01`

> 작성일: 2026-08-04 · 상태: **프로토타입 구현 완료 (온라인 2인 협동까지)** · 산출물: `index.html` (단일 파일)
>
> 아래 수치 중 ★ 표시는 헤드리스 Chromium 자동 플레이로 **실측 후 조정된 값**입니다.

---

## 1. 한 줄 정의

브라우저에서 바로 실행되는 **동방류 스펠카드 탄막 슈팅** 프로토타입.
F-22로 MiG 편대와 보스를 요격하는 고고도 공중전. 단일 HTML 파일, 외부 에셋 0개,
PC 키보드와 모바일 터치 양쪽 지원.

---

## 2. 확정 사항 요약

| 항목 | 결정 |
|---|---|
| 기술 스택 | HTML5 Canvas 2D + 바닐라 JS (단일 `.html` 파일) |
| 장르 | 동방류 — 보스 스펠카드 중심 |
| 범위 | 플레이 가능한 프로토타입 (핵심 루프 완성) |
| 조작 | PC 키보드 + 모바일 터치 동시 지원 |
| 그래픽 | 코드 생성 벡터 스프라이트 — F-22 / MiG 3종 / 미사일 (외부 이미지 0개) |
| 멀티플레이 | 온라인 2인 협동 (WebRTC P2P · 방 코드 4자리) ★ |
| 사운드 | WebAudio 합성 효과음 (BGM 없음) |
| 테마 | 고고도 하늘 (성층권 그라데이션 + 흐르는 구름층) |
| 핵심 기정 | 저속 이동, 그레이즈, 봄, 잔기 + 무적시간 |
| 보스 | 1체 / 스펠카드 3단계 |
| 난이도 | Easy / Normal / Hard 선택 |

---

## 3. 게임 루프

```
타이틀 화면
  └ 난이도 선택 (Easy / Normal / Hard)
      └ STAGE 1 — 잡몹 구간 (약 60초)
          ├ WAVE 1  직진형 편대 ×2
          ├ WAVE 2  사인파 편대 + 직진형 편대 ×2
          ├ WAVE 3  포탑형 편대 + 사인파 편대 + 직진형 편대
          └ MID-BOSS
              ↑ 이 구간 내내 파워 아이템을 파밍해 화력을 키운다
      └ 보스전 — 스펠카드와 요격 구간이 번갈아 진행 ★
          ├ 논스펠 (통상탄막)
          ├ INTERCEPT 1   직진형 편대 ×3            (약 17초)
          ├ 스펠카드 1 「환상의 회전목마」
          ├ INTERCEPT 2   사인파 + 직진형 편대 ×4   (약 22초)
          ├ 스펠카드 2 「추격의 십자포화」
          ├ INTERCEPT 3   포탑형 + 사인파 + 직진형  (약 21초)
          └ 스펠카드 3 「종막・만개」
              └ 격파 → 리절트 화면
                   또는 잔기 0 → 게임오버 → 리트라이
```

### ★ 요격 구간(INTERCEPT)을 넣은 이유
스펠카드를 연달아 4개 붙이면 긴장이 계속 걸린 채 유지돼 지치기 쉽습니다.
사이에 잡몹 구간을 끼우면 (1) 숨 돌릴 구간이 생기고, (2) 피격으로 잃은 파워를 만회할
기회가 생기며, (3) 다음 스펠카드가 다시 '등장'하는 리듬이 만들어집니다.
요격 구간 동안 보스는 위로 물러났다가 다음 스펠카드에서 부드럽게 재진입합니다.

- 1회 플레이 목표 시간: **3〜4분** (무피격 자동 플레이 실측 3분 19초〜3분 36초)
- 리트라이는 타이틀 경유 없이 즉시(1키/탭) 가능 — 반복 플레이가 핵심
- **핵심 구조: 스테이지에서 얼마나 파밍했는지가 보스전 길이를 결정한다** (§4-A 참조)

---

## 4. 자기(플레이어) 사양

| 항목 | 값 | 비고 |
|---|---|---|
| 히트박스 | 반경 **8.25px** ★ | 주익 포함 기체 실루엣에 맞춤. **상시 표시**(분홍 원) |
| 표시 크기 | F-22 실루엣 약 20×25px ★ | 원본의 55% (`PLAYER_SCALE = 0.55`). 적기는 70%로 별도 유지 |
| 고속 이동 | 5.0 px/frame | 기본 이동 |
| 저속 이동 | 2.0 px/frame | Shift 유지 / 모바일은 자동 판별 |
| 그레이즈 반경 | 28.25px (판정 + 20px 밴드) | 이 안에 탄이 들어오면 그레이즈 1회 (탄당 1회) |
| 잔기 | 3 (Easy 4 / Hard 2) | |
| 봄 | 3 시작 / 최대 5 (아이템으로 보충) | 잔기 소모 시 3으로 리셋 |
| 피격 무적 | 2.0초 (점멸 표시) | 피격 시 화면 내 탄 전부 소거 |
| 발사 | 자동 연사 (PC는 Z 유지 or 자동) | 프로토타입은 **자동 발사** 기본 |
| 자탄 | 정면 2줄 + 옵션 포드 0〜6기 | 파워 레벨에 따라 변동 (§4-A) |

### ★ 히트박스를 에셋에 맞춘 결과

동방류의 관례는 "보이는 기체는 크고, 판정은 아주 작다(3px)"입니다.
이번에는 **판정을 기체 실루엣에 맞추기로** 했고, 그 결과 피격 면적이 약 12배 커졌습니다
(반경 3px → 8.25px). 이건 단순한 숫자 교체가 아니라 **게임 전체를 다시 잡아야 하는 변경**입니다.

함께 조정한 것:

| 항목 | 이전 | 지금 | 이유 |
|---|---|---|---|
| 전역 탄 밀도 | 1.00 | **0.75** | 넓어진 몸으로 지나갈 틈이 필요 |
| 전역 탄 속도 | 1.00 | **0.82** | 읽고 반응할 시간 확보 |
| 링 탄 수 | 12 / 11 | 10 / 9 | 링 사이 간격 확보 |
| 조준 산탄 | 5-way | 3-way (각도는 확대) | 정면이 막히지 않도록 |
| 낙하벽 | 13발 중 2발 틈 | 9발 중 3발 틈 | 통과 가능한 폭 유지 |
| 페이즈 제한시간 | 40초 | 50초 | 회피 우선 플레이도 데미지로 끝낼 수 있게 |
| 히트박스 표시 | 저속 시에만 | **상시 표시** | 판정이 커진 만큼 항상 보여야 함 |

### 저속 모드 판정
- PC: `Shift` 유지 중
- 모바일: 드래그 이동 속도가 임계값 이하일 때 자동으로 저속 판정 + 히트박스 표시

---

## 4-A. ★ 파워 시스템 (파밍 보상)

게임의 중심 축입니다. **스테이지에서 파밍을 잘하면 보스전이 짧아지고, 안 하면 길어집니다.**

| 파워 Lv | 발사 간격 | 볼리당 탄 수 | 탄 데미지 | 옵션 포드 | 이론 DPS |
|---|---|---|---|---|---|
| 0 | 5프레임 | 2 | 1.00 | 0기 | 24 |
| 1 | 4프레임 | 4 | 1.35 | 2기 | 81 |
| 2 | 3프레임 | 6 | 1.35 | 4기 | 162 |
| 3 | 3프레임 | 6 | 1.70 | 4기 | 204 |
| 4 (MAX) | 3프레임 | 8 | 1.70 | 6기 | 272 |

이전 표(24/60/90/120/160) 대비 **최대 1.7배 상향**. 탄 수뿐 아니라 발당 데미지도 함께
올려서, 포드가 늘지 않는 레벨(L2→L3)에서도 체감이 생기도록 했습니다.

### 획득 난이도

| 항목 | 값 |
|---|---|
| 아이템 1개당 상승량 | +0.062 |
| 자석 반경 | 105px (가까울수록 강하게 끌림) |
| 획득 반경 | 26px |
| 낙하 최대 속도 | 3.1 px/frame |
| 스테이지 전체 드랍량 | 약 95개 |

드랍 개수: 직진형 1 · 사인파형 2 · 포탑형 3 · 편대 전멸 3 · 중간보스 10
(요격 구간이 3개 추가되면서 런당 총 드랍량은 약 95개 → 약 160개로 늘었습니다)

- 실측: **적극 파밍 시 보스 진입 시점에 MAX / 방치하면 Lv.3**
- 자석 반경과 드랍 개수를 함께 늘렸으므로 `POWER_PER_ITEM`은 사실상 정규화 상수입니다.
  체감 난이도를 바꾸려면 이 값보다 **자석 반경**을 만지는 편이 효과가 직접적입니다.

### ★ 옵션 포드는 안쪽으로 수렴시킨다
초안에서는 포드를 바깥으로 벌렸는데, 그 결과 **파워를 67% 올려도 보스 클리어가 9%밖에
안 빨라졌습니다** — 포드 탄이 전부 보스 옆을 스쳐 지나갔기 때문입니다.
포드를 살짝 안쪽으로 수렴시키자 같은 조건에서 보스 구간이 103초 → 68초로 벌어졌고,
그제서야 파밍이 의미를 갖게 되었습니다.

### ★ 보조 아이템 3종 (파워업 다양화)

파워 아이템과 별개로, **편대 전멸(55% 확률) · 포탑형 격파(25%) · 중간보스(2개)** 에서
드물게 떨어집니다. 지금 가장 아쉬운 것을 우선 주도록 가중치를 두었습니다.

| 아이템 | 배지 | 효과 |
|---|---|---|
| 유도 미사일 | 청록 `H` | 13초간 10프레임마다 유도 미사일 2발 자동 발사 (발당 5 데미지, 최대 26초까지 누적) |
| 봄 보충 | 노랑 `B` | 봄 +1 (최대 5) |
| 실드 | 보라 `S` | 피격 1회를 대신 막음 (최대 3). **잔기도 파워도 잃지 않음** |

- 실드는 자기 주위 점선 링으로 표시되고, 깨지면 화면 내 탄이 소거됩니다
- 유도 미사일은 가장 가까운 적 → 없으면 보스를 추적. 요격 구간에서 특히 강력합니다
- 실측 1회 플레이당 획득량: 유도 3〜7 / 봄 2〜3 / 실드 3〜4

### ★ 피격 시 파워 한 단계 강등
- 피격하면 **파워가 바로 아래 레벨의 바닥까지** 떨어집니다 (예: Lv.3에서 3.8 → **2.0**)
  - 단순히 1.0을 빼는 게 아니라 **레벨 게이지가 눈에 띄게 한 칸 내려가도록** 했습니다
  - 실측: 3.8→2.0 · 3.0→2.0 · 2.4→1.0 · 1.0→0.0
- 잃은 만큼이 자기 위쪽으로 흩뿌려짐 (최대 18개)
- 흩뿌려진 아이템은 **다시 주울 수 있음** — 즉사 후 만회의 여지를 남김
- 파워 아이템은 점수 아이템과 달리 **자석 반경이 좁아(105px) 직접 주우러 가야 함**
  → 이것이 '파밍'이라는 행위를 성립시키는 장치

---

## 4-B. ★ 스테이지 구간 (STAGE 1, 약 60초)

### 적 타입

| 타입 | 기체 | HP | 행동 | 파워 드랍 |
|---|---|---|---|---|
| 직진형 | **MiG-21** (델타익, 적주황) | 4 ★ | 대각선으로 낙하하며 조준탄 1발씩 | 1 |
| 사인파형 | **MiG-29** (쌍수직미익, 분홍) | 7 ★ | 정위치까지 내려온 뒤 좌우로 흔들리며 조준 3-way | 2 |
| 포탑형 | **MiG-31** (대형 요격기, 보라) | 13 ★ | 정지 후 전방위 링 3회 발사 → 위로 이탈 | 3 |
| 중간보스 | 백색 코어 + 회전 링 | 3,200 | 5초마다 나선 링 ↔ 조준 산탄 전환 | 10 |

> 보스와 중간보스는 항공기가 아닌 **추상 형태(코어 + 회전 링)** 로 남겼습니다.
> 탄막의 '발사원'이라는 역할이 기체 실루엣보다 명확하게 읽히기 때문입니다.

- 적 몸체는 **무해** (탄에만 피격 판정) — 동방 관례
- 피격당한 적은 흰 테두리 호(arc)로 남은 체력을 표시

### 편대 전멸 보너스
- 한 편대를 **한 마리도 놓치지 않고** 전부 격파하면 **+20,000점 + 파워 3개 + 보조 아이템(55%)**
- 한 마리라도 화면 밖으로 도망가면 그 편대는 보너스 대상에서 제외
  → 흘려보내지 않고 적극적으로 쓸어 담게 만드는 장치
- 실측: 적극 파밍 플레이 6회 / 사격 집중 플레이 8회 전멸 달성
  (파밍하러 움직이면 놓치는 편대가 생기는 **트레이드오프**가 자연 발생)

### 봄 사용 시
스테이지 구간에서 봄을 쓰면 화면 내 모든 적에게 **초당 45 데미지** — 위기 탈출 겸 편대 정리용

---

## 4-C. ★ 온라인 2인 협동

### 화면 흐름
```
타이틀 → [ONLINE 2P] (또는 O 키)
  └ MODE  ─ SOLO / CREATE ROOM / JOIN ROOM
      ├ CREATE ROOM → 4자리 코드 자동 생성 → LOBBY(호스트)
      └ JOIN ROOM   → 코드 4자리 입력 → LOBBY(참가자)
          └ LOBBY ─ 양쪽 READY → 호스트가 START → PLAY
                    (난이도는 호스트가 지정)
```
- 방 코드는 `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` 4자리 (헷갈리는 `O0I1` 제외)
- 게임오버·클리어 시 각자 리트라이하면 어긋나므로 **대기실로 함께 복귀**합니다

### ★ 왜 단일 HTML만으로는 안 되는가
두 브라우저를 잇는 **시그널링(중계)** 이 반드시 필요합니다. 그래서 이 기능만큼은
"외부 의존 0개" 원칙이 깨집니다 — PeerJS 공개 브로커로 방 코드를 교환하고,
연결된 뒤의 게임 데이터는 **WebRTC로 두 브라우저가 직통**합니다.

- ★ **PeerJS(91KB)를 HTML에 인라인**했습니다 → 실행 중 외부 스크립트를 받아오지 않습니다.
  (파일 크기 148KB → 242KB. 여전히 단일 파일)
- 남는 외부 의존은 **시그널링 브로커 한 곳뿐**이며, 방 코드 교환에만 쓰입니다.
  연결이 성립하면 게임 데이터는 두 브라우저가 직통합니다.
- **공개 브로커가 막힌 망(사내·학교)을 위한 탈출구**: `?peer=호스트:포트:경로` 파라미터로
  자체 PeerServer(`npx peerjs --port 9000`)를 지정할 수 있습니다.
- 자체 서버로 완전히 갈아타야 한다면 `Transport` 인터페이스만 교체하면 됩니다.

### 배포
`index.html` 하나만 정적 호스팅에 올리면 됩니다. 온라인 모드는 **HTTPS 주소 권장**
(`file://`은 브라우저에 따라 WebRTC가 제한될 수 있음). 구체적인 절차는 `README.md` 참조.

### ★ 동기화 구조 — 호스트 권위 + 클라이언트 예측

| 주체 | 담당 |
|---|---|
| 호스트 | 탄·적·보스·아이템·웨이브 전부 시뮬레이션. **양쪽 자탄까지 호스트가 발사** |
| 클라이언트 | 자기 기체 위치·자기 피격·자기 아이템 획득만 로컬 권위 |

- 호스트 → 클라이언트: **바이너리 스냅샷 30Hz** (탄 6B, 적 6B, 아이템 5B) ≈ 1.2KB/틱 ≈ 36KB/s
- 클라이언트 → 호스트: 자기 기체 상태 20B, 60Hz
- 봄·피격·아이템 획득 같은 드문 사건만 JSON 이벤트

**왜 이 구조인가**: 탄 150발을 매 프레임 보내는 건 대역폭이 감당되지만,
자기 기체가 1 RTT 늦게 움직이면 탄막게임은 플레이가 불가능합니다. 그래서
**자기 기체와 자기 피격만 클라이언트가 직접 판정**합니다(친구끼리 하는 협동이라
치팅은 고려하지 않았습니다). 반대로 자탄은 호스트가 쏘게 해서
**데미지 판정을 한 곳으로 모았습니다** — 자탄이 조금 늦게 보이는 건 체감되지 않습니다.

**결정론적 락스텝을 쓰지 않은 이유**: 패턴이 `Math.random()`에 의존하고
`Math.sin/cos`는 브라우저 엔진마다 결과가 다를 수 있어, 시드 PRNG와 자체 삼각함수
테이블까지 필요합니다. 스냅샷 방식이 대역폭을 감당할 수 있어 그쪽을 택했습니다.

### 협동 규칙
- **잔기·파워 개별**. 아이템은 먼저 줍는 사람이 가져갑니다
- 잔기를 다 잃으면 **관전** 상태 → **다음 구간(스펠카드/요격) 시작 시 잔기 1로 부활**
- **둘 다 동시에 관전이면 게임오버**
- 봄은 전체 화면 효과라 상대 화면에서도 탄이 지워집니다

### 테스트 방법
자동 테스트는 공개 브로커에 의존하지 않도록 `?net=bc` 로 **BroadcastChannel 전송**을
쓸 수 있습니다(같은 오리진 탭 두 개). `?net=bc&host=CODE` / `?net=bc&join=CODE` 로
대기실에 바로 진입합니다. 실제 플레이는 파라미터 없이 열면 P2P가 쓰입니다.

---

## 5. 조작 매핑

### PC (키보드)
| 키 | 동작 |
|---|---|
| ← ↑ → ↓ / WASD | 이동 |
| Shift | 저속 이동 (히트박스 표시) |
| Z | 발사 (자동 발사 ON이면 보조) |
| X | 봄 |
| Esc / P | 일시정지 |
| O | 온라인 2인 모드 진입 (타이틀에서) |
| R | 즉시 리트라이 (게임오버·클리어 시) |

### 모바일 (터치)
- **가상 조이스틱 없음.** 화면 아무 곳이나 터치 → 자기가 손가락에 **오프셋 추적**
  (손가락 위쪽 60px에 자기가 위치 → 손가락이 자기를 가리지 않음)
- 발사: 자동
- 봄: 우하단 고정 버튼 (반경 44px, 남은 개수 표시)
- 일시정지: 좌상단 버튼

---

## 6. 보스 사양

### 6.1 공통
- 위치: 화면 상단 1/4 지점, 페이즈마다 좌우 이동 패턴 상이
- HP 게이지: 상단 바 + 남은 페이즈 수 도트 표시
- 페이즈 전환 시: 화면 내 탄 전부 소거 + 0.8초 연출 (스펠카드 이름 좌측 슬라이드 인)
- 각 페이즈 **제한시간 50초** ★ — 초과 시 페이즈 강제 종료(타임아웃, 보너스 없음)
- ★ 페이즈당 HP **2,900 × 난이도 배율 × 페이즈 보정** · 보스 피격 반경 40px
- 요격 구간 동안 보스는 화면 위로 물러나며 무적. 다음 스펠카드에서 부드럽게 재진입

### ★ 페이즈별 HP 보정 — `[1.05, 1.70, 0.68, 1.15]`

보스가 빠르게 움직이는 페이즈일수록 명중률이 떨어져, 같은 HP를 줘도 소요 시간이
**10초〜26초로 들쭉날쭉**했습니다 (Phase 1은 보스가 거의 안 움직여 10초 만에 끝나고,
Phase 2는 좌우로 크게 흔들려 26초). 실측 소요시간을 역산해 페이즈별 HP를 보정했습니다.

보정 후 Normal/파밍 기준: **18.4초 → 19.0초 → 19.3초 → 19.3초** — 고르게 나옵니다.

### 6.2 페이즈 구성

**Phase 0 — 논스펠 「난사」**
- 보스가 좌우로 사인파 이동하며 자기 조준 3-way 산탄 발사
- 동시에 화면 상단에서 무작위 낙하탄
- 목적: 기본 이동·그레이즈 감각 익히기

**Phase 1 — 스펠카드 「환상의 회전목마」 (Rotating Ring)**
- 보스 중심에서 N개의 탄이 링 형태로 방출, 방출각이 매 웨이브마다 골든앵글만큼 회전
- 나선 무늬가 생기며 **틈이 규칙적으로 열림** — 틈을 읽고 파고드는 패턴
- 파라미터: 웨이브당 탄 수 12 / 간격 0.25초 / 탄속 2.2

**Phase 2 — 스펠카드 「추격의 십자포화」 (Aimed Cross)**
- 자기를 향한 조준 5-way + 빠른 추격 3-way
- 화면 좌우 벽에서 수평 레이저성 직선탄 (가로축)
- ★ **틈이 하나 열린 낙하벽**(13발 중 2발 자리 비움, 세로축) 추가
  - 초안은 밀도가 avg 14로 지나치게 비어 있었음 → 세로 낙하벽을 넣어
    '가로 레이저 × 세로 낙하' 구조가 실제로 십자 형태가 되도록 보강
- 조준탄은 **발사 순간의 자기 위치**를 노리므로 계속 움직여야 함

**Phase 3 — 스펠카드 「종막・만개」 (Finale Bloom)**
- Phase 1 + Phase 2 동시 진행 + 랜덤 폭발탄(도착 지점에서 2차 방사)
- ★ 밀도 최대가 되도록 간격을 13프레임으로 압축 (초안에서는 Phase 1보다 오히려 한산했음)
- 봄 사용을 전제로 설계
- 클리어 시 화면 전체 탄이 **점수 아이템으로 변환**되며 흡수되는 연출

---

## 7. 난이도 배율

| 파라미터 | Easy | Normal | Hard |
|---|---|---|---|
| 탄 속도 배율 | 0.75× | 1.0× | 1.3× |
| 탄 밀도 배율 | 0.7× | 1.0× | 1.4× |
| 보스 HP 배율 | 0.8× | 1.0× | 1.2× |
| 시작 잔기 | 4 | 3 | 2 |
| 그레이즈 점수 배율 | 1.0× | 1.5× | 2.5× |

> 난이도는 **패턴 자체를 바꾸지 않고 배율만 조정** — 구현 부담 최소화 + 밸런싱 용이

---

## 8. 스코어 시스템

| 행동 | 점수 |
|---|---|
| 그레이즈 1회 | 100 × 난이도 배율 |
| 적 탄 소거 (봄/피격) | 10 / 탄 |
| 보스 데미지 | 50 / 1 데미지 |
| ★ 잡몹 격파 | 3,000 (포탑형 5,000) |
| ★ 중간보스 격파 | 80,000 |
| ★ 편대 전멸 | 20,000 |
| ★ 파워 아이템 획득 | 200 |
| 페이즈 노미스 클리어 | +50,000 |
| 페이즈 노봄 클리어 | +30,000 |
| 잔기 잔여 보너스 | 잔기 × 100,000 |
| 봄 잔여 보너스 | 봄 × 30,000 |

- 봄 사용 시 **현재 그레이즈 콤보 리셋** (봄에 대한 소프트 페널티)
- 최고 점수는 메모리 내에서만 유지 (브라우저 저장소 미사용)

---

## 9. 봄 사양

- 발동: `X` / 모바일 봄 버튼
- 효과:
  1. 즉시 화면 내 모든 탄 소거 (점수 아이템으로 전환)
  2. 발동 후 **3초간 무적**
  3. 3초간 보스에게 초당 30 데미지
  4. 확산 충격파 링 연출 (반경 확장 + 화면 플래시)
- 쿨타임 없음. 개수만 소모
- 피격 판정 직전 **0.15초 유예(데스봄)** — 맞은 직후 봄을 누르면 피격 무효

---

## 10. ★ 시각 설계 — 고고도 하늘 + 항공기

### 배경 (하늘)
- 세로 그라데이션: 위 `#02040e`(성층권) → 중간 `#0b2a54` → 아래 `#123c6f`
- 상층부에 **별** 46개가 천천히 흐름 (아래로 갈수록 페이드아웃)
- 중·하층에 **구름층** 8덩이가 아래로 흘러 전진하는 느낌을 줌 (알파 0.10〜0.23)
- 아래쪽이 지나치게 밝아지면 탄이 안 보이므로 **밝기를 의도적으로 억제**했습니다.
  이것이 "고고도 짙은 하늘"을 고른 이유입니다 — 주간 청공이었다면 탄 색을 전부
  다시 잡아야 했습니다.

### 오브젝트

| 요소 | 색 | 형태 |
|---|---|---|
| 자기 | **밝은 하늘빛 그라데이션**(`#eaf8ff`→`#7fb2cf`) + 흰 엣지 | **F-22 랩터** (다이아몬드 주익 / 쌍수직미익 / 캐노피 / 노즐 2개 + 애프터버너) |
| 자기 히트박스 표시 | 분홍 `#ff5c7a` | 상시 얇은 원, 저속 시 강조 + 채움 |
| 옵션 포드 | 연청 `#9df0ff` | 소형 무인기 (파워 레벨만큼 V자 편대) |
| 자탄 | 연청 `#7dffff` | 예광탄 캡슐 |
| 유도 미사일 | 연청 `#7dffff` | 미사일 + 연기 파티클 |
| 적탄(링) | 마젠타 `#ff2fd0` | **미사일** (노즈콘 + 후미 핀 + 배기 화염) |
| 적탄(낙하) | 분홍 `#ff5ce0` | 미사일 (소형) |
| 적탄(폭발 씨앗) | 주황 `#ff8a3d` | 미사일 (대형, 맥동) |
| 적탄(조준) | 앰버 `#ffb020` | 예광탄 |
| 적탄(레이저) | 바이올렛 `#a24bff` | 긴 예광탄 |
| 적탄(산탄) | 마젠타 | 둥근 예광탄 |
| 아이템(파워) | 초록 `#5cff9e` | **각진 배지 `P`** |
| 아이템(점수) | 하늘 `#59b8ff` | 각진 배지 `★` |
| 아이템(유도/봄/실드) | 청록/노랑/보라 | 각진 배지 `H` / `B` / `S` |

### ★ 미사일과 아이템을 형태로 분리한다
아이템을 원형·발광으로 두면 미사일과 섞여 "먹어야 할 것"과 "피해야 할 것"이
순간적으로 구분되지 않습니다. 그래서 아이템은 전부

- **각진 라운드 사각 배지 + 흰 테두리 + 글자**  (탄은 전부 뾰족하거나 둥근 형태)
- **탄이 쓰지 않는 색 계열** (초록 / 하늘 / 보라)
- **좌우로 흔들리는 움직임** (탄은 등속 직선)

세 가지를 동시에 다르게 만들었습니다. 색만 바꾸면 밀도가 높을 때 다시 헷갈립니다.

### ★ 밝기 조정
1차 항공기 에셋은 동체 그라데이션이 `#9ad0e8 → #243a4c`로, **하늘 배경에서 실루엣이
어둡게 묻혔습니다.** `#eaf8ff → #7fb2cf`로 올리고 엣지 스트로크를 흰색에 가깝게,
기수에 흰 하이라이트를 추가했습니다. 적기(MiG)도 같은 이유로 어두운 쪽 스톱을
한 단계씩 올렸습니다 — 기체가 70%로 작아지면서 어두우면 더 안 보이기 때문입니다.

### ★ 후처리 블룸

발광이 스프라이트 안에 구워진 글로우뿐이라 빛이 스프라이트 경계에서 끊겼습니다.
지금은 **발광 오브젝트만 1/4 크기 캔버스에 다시 그린 뒤, 1/8로 한 번 더 줄여
`lighter`로 화면에 합성**합니다. 양선형 보간이 블러 역할을 해서 별도 블러 필터가 필요 없습니다.

시도했다가 버린 방법 두 가지 (둘 다 실측으로 탈락):

| 방법 | 200탄 FPS | 문제 |
|---|---|---|
| `ctx.filter='blur()'` + 전체 화면 | 60 → **21** | 소프트웨어 렌더러에서 블러 필터가 지나치게 비쌈 |
| 메인 캔버스를 축소 복사(`drawImage(cv,…)`) | 60 → **30** | GPU 텍스처 리드백이 걸림 |
| **발광 오브젝트만 재렌더** (채택) | 55 → **54** | 비용 거의 없음. 배경이 안 번져 그림도 더 깨끗함 |

HUD보다 먼저 호출해서 UI 글자는 번지지 않게 했습니다.

### ★ 기체 뱅킹
좌우 이동량을 평활해 롤 각으로 바꾸고, 기수를 살짝 틀면서(`rotate`) 주익 폭을 눌러
(`scaleX = cos(bank)`) 3D처럼 보이게 했습니다. **판정 원과 실드 링은 뱅킹 변환 밖에서**
그려 항상 원형을 유지합니다 — 판정 표시가 기울면 플레이어가 오독합니다.

### ★ 폭발 이펙트
단색 사각 파티클 하나였던 것을 4종 합성으로 바꿨습니다.

| 종류 | 표현 | 렌더 방식 |
|---|---|---|
| 충격파 링 | 확장하며 얇아지는 원 | `arc` + ease-out |
| 스파크 | 속도 방향 스트릭 | **색상·굵기별로 묶어 한 번에 stroke** |
| 연기 | 퍼지며 사라지는 덩어리 | 미리 구운 방사형 스프라이트 |
| 파편 | 회전하는 조각 | 미리 구운 칩 스프라이트 + `setTransform` |

`explode(x, y, 배율, 뜨거운색, 식은색)` 하나로 잡몹·보스·자기 격추를 모두 커버합니다.

### ★ 미사일 비율 주의
1차 구현에서 **핀을 몸통 폭의 3배로 그렸더니 미사일이 아니라 별(✳) 모양**으로
보였습니다. 핀은 몸통 폭의 0.6배까지만, 전장은 폭의 3.5〜4배로 잡아야
"길쭉한 미사일"로 읽힙니다.
| 보스 / 중간보스 | 백색 코어 + 회전 링 | 다중 링 회전 (항공기 아님) |
| MiG-21 | 적주황 `#ff6b5c` | 델타익 + 단일 수직미익 |
| MiG-29 | 분홍 `#ff9ad5` | 쌍수직미익 + LERX |
| MiG-31 | 보라 `#c77dff` | 대형 각진 동체 + 사각 인테이크 |
| 그레이즈 이펙트 | 흰 스파크 파티클 | |

### 필수 연출
- 모든 탄은 `shadowBlur` 글로우 — 어두운 배경에서 탄 궤적이 선명하게 보이도록
- 탄 코어는 **흰색 채움** — 배경과 겹쳐도 판정 위치가 명확
- 피격 시 화면 셰이크 + 적색 비네트
- 스펠카드 선언 시 이름 텍스트 슬라이드 인 + 배경 색조 전환

### 가독성 원칙 (탄막게임의 생명)
1. 자탄은 적탄보다 **어둡고 얇게** — 적탄을 가리지 않음
2. 파티클 이펙트는 적탄보다 **투명도 낮게**
3. 배경 그리드는 최대 밝기 12% 이하

---

## 11. 사운드 (WebAudio 합성)

| 이벤트 | 합성 방식 |
|---|---|
| 자탄 발사 | 짧은 사각파 blip, 8ms, 볼륨 낮게 (연사라 시끄럽지 않게) |
| 그레이즈 | 고음 사인파 틱 + 피치 상승 (콤보에 따라 음 상승) |
| 적탄 발사 | 노이즈 버스트 필터링 |
| 피격 | 저음 사각파 하강 + 노이즈 |
| 봄 | 저음 스윕 + 화이트 노이즈 페이드 |
| 스펠카드 선언 | 3음 아르페지오 |
| 보스 격파 | 하강 스윕 + 다중 폭발음 |

- 마스터 볼륨 슬라이더 (타이틀에서 조절)
- 최초 사용자 입력 시 AudioContext resume (브라우저 정책 대응)

---

## 12. 화면 구성

```
┌──────────────────────────────────┐
│ SCORE 1,234,560      GRAZE 482   │  ← 상단 HUD
│ ▓▓▓▓▓▓▓▓░░░░░  ● ● ○             │  ← 보스 HP / 페이즈 도트
├──────────────────────────────────┤
│                                  │
│              [보스]              │
│                                  │
│         PLAY FIELD               │  ← 480 × 640 논리 해상도
│                                  │
│              [자기]              │
│                                  │
├──────────────────────────────────┤
│ ♥♥♥   ★★★              00:47     │  ← 잔기 / 봄 / 타이머
└──────────────────────────────────┘
```

- **논리 해상도 480×640 고정**, 화면 크기에 맞춰 스케일링 (letterbox)
- 모바일 세로 화면에 자연스럽게 맞음
- 데스크톱에서는 좌우에 스펠카드 정보 사이드 패널 표시

---

## 13. 기술 설계

### 파일 구조
```
game_test01/
├── DESIGN.md      ← 이 문서
└── index.html     ← 게임 전체 (HTML + CSS + JS 인라인)
```

### 핵심 아키텍처

**탄 관리 — 오브젝트 풀 + 플랫 배열**
- 탄 최대 3,000개 사전 할당, GC 압력 제거
- `Float32Array` 기반 SoA(Structure of Arrays) 고려 — 단, 프로토타입은 가독성 우선으로 오브젝트 풀 사용

**패턴 정의 — 데이터 주도**
```js
// 패턴을 제너레이터 함수로 정의 → 프레임마다 next() 호출
function* rotatingRing(cfg) {
  let angle = 0;
  while (true) {
    for (let i = 0; i < cfg.count; i++) {
      spawnBullet(bossX, bossY, angle + i * TAU / cfg.count, cfg.speed);
    }
    angle += GOLDEN_ANGLE;
    yield cfg.interval;   // 다음 발사까지 대기 프레임
  }
}
```
> 제너레이터 방식이면 새 스펠카드 추가가 함수 하나 추가로 끝남 — 확장성 확보

**게임 루프**
- 고정 타임스텝 **60 FPS** (`accumulator` 방식), 렌더는 가변
- 저사양 기기에서 슬로우다운은 허용하되 **판정은 프레임 단위 고정** (탄막게임 필수)

**충돌 판정**
- 자기 vs 적탄: 원-원 거리 제곱 비교 (sqrt 미사용)
- 자탄 vs 보스: 보스 히트박스는 원 1개
- 공간 분할 불필요 (자기 1개 vs 탄 N개 = O(N), 3000개면 충분히 빠름)

### ★ 성능 실측 (헤드리스 Chromium = 소프트웨어 렌더러, 실기기 GPU보다 크게 불리한 조건)

합성 스트레스 테스트(강제로 탄을 뿌리는 방식)보다 **실제 플레이 60초 프레임 분포**가
더 의미 있는 지표라 판단해, 지금은 후자로 측정합니다. (Hard, 회피 AI, 3,600프레임)

| 지표 | 값 |
|---|---|
| 중앙값 | **54 FPS** |
| 하위 5% | 41 FPS |
| 하위 1% | 35 FPS |
| 최대 동시 파티클 | 66 |

이펙트·블룸을 넣기 전에는 중앙값이 60이었습니다. 원인을 분해해 보니 **블룸은 1 FPS 수준이고
비용의 대부분은 파티클**이었습니다(파티클만 끄면 55 → 32 FPS 차이). 연기 개수·수명·반경을
줄이고 파티클 풀 스캔을 커서 방식으로 바꿔 48 → 54 FPS로 회복했습니다.

**자동 품질 조정**: 평균 55 FPS 아래로 떨어지면 파티클 밀도를 단계적으로 낮추고,
그래도 모자라면 블룸을 끕니다. 위 측정에서도 실제로 작동해 파티클 밀도가 0.3까지 내려갔습니다
— 즉 **느린 기기에서는 스스로 가벼워집니다.**

> 위 수치는 전부 헤드리스 소프트웨어 렌더러(SwiftShader) 기준이라 실기기 GPU보다 크게 불리합니다.
> 실제 브라우저에서는 파티클 밀도가 1.0으로 유지될 가능성이 높습니다.

초안의 "2,000탄 60 FPS" 목표는 소프트웨어 렌더러에서 검증 불가했고, 게임이 그 수치를
만들지도 않으므로 **실사용 구간(≤200탄) 60 FPS 유지**로 목표를 재정의했습니다.

적용한 최적화:
- 글로우 스프라이트를 미리 구워 `shadowBlur` 회피 (필레이트 절약을 위해 패딩 2.4×로 축소)
- 항공기·미사일·배지도 전부 초기화 시 오프스크린 캔버스로 **1회 베이킹** (매 프레임 패스 재구성 없음)
- 적탄은 등속 직선이라 **진행 방향 회전값을 생성 시 1회만 계산해 캐시** (유도 미사일만 매 프레임 갱신)
- 탄 관리에 **프리 스택 + 액티브 배열 스왑 제거** 도입 → 매 프레임 3,000슬롯 전수 순회 제거
- 회전이 필요 없는 탄(대다수)은 `save/restore` 없이 `drawImage` 직행
- FPS가 48 아래로 떨어지면 파티클 밀도 자동 감소 (25% 단위)

---

## 14. 프로토타입 완료 기준 (Definition of Done) — ★ 자동 테스트로 검증 완료

- [x] 타이틀 → 난이도 선택 → 플레이 → 리절트 → 리트라이 전체 흐름 동작
- [x] 자기 이동(고속/저속), 히트박스 표시, 자동 발사
- [x] 그레이즈 판정 및 점수 반영
- [x] 봄 발동 (탄 소거 + 무적 + 데미지) + 데스봄
- [x] 잔기 소모 및 게임오버
- [x] ★ 스테이지 3웨이브 + 중간보스 → 보스전 전체 흐름 동작
- [x] ★ 적 3타입 동작, 편대 전멸 보너스 판정 (도망친 편대는 제외되는 것까지 확인)
- [x] ★ 파워 아이템 파밍 → 화력 상승 → 보스전 단축이 실측으로 확인됨
- [x] ★ 논스펠→요격→스펠1→요격→스펠2→요격→스펠3 순서 동작, 보스 퇴각/재진입 연출
- [x] ★ 보조 아이템 3종(유도 미사일 / 봄 / 실드) 획득·발동·소진 전 과정
- [x] ★ 실드가 피격을 대신 막고 잔기·파워를 보존하는지
- [x] ★ 항공기·미사일·하늘 배경 렌더링 (F-22 / MiG-21 / MiG-29 / MiG-31)
- [x] ★ 후처리 블룸 · 기체 뱅킹 · 폭발 이펙트 4종 합성
- [x] ★ 저사양 자동 품질 강등(파티클 → 블룸 순) 작동 확인
- [x] ★ 온라인: 방 생성 → 참가 → 양쪽 READY → START 전체 흐름
- [x] ★ 온라인: 월드 동기화 (호스트 적 5기 = 클라이언트 스냅샷 5기), 서로의 기체 위치 교차 확인
- [x] ★ 온라인: 전멸 → 관전 → 다음 구간 부활 (잔기 1로 복귀)
- [x] ★ 솔로 모드 회귀 (온라인 추가 후에도 기존 흐름 이상 없음)
- [x] ★ 피격 시 파워 손실 및 흩뿌린 아이템 재획득
- [x] 보스 4페이즈(논스펠 + 스펠카드 3) 전부 동작 — 전 페이즈 HP 격파로 종료 확인
- [x] 난이도 3종 배율 적용 — 탄 밀도가 Easy < Normal < Hard로 단조 증가함을 실측
- [x] WebAudio 효과음 전체
- [x] 모바일 터치 조작 + 반응형 스케일링 (390×844 뷰포트 실행 확인)
- [x] 실사용 구간 60 FPS 유지 확인
- [x] 콘솔 에러 0건 (데스크톱·모바일 양쪽)

### ★ 실측 밸런스 (자동 플레이 = 타깃을 x축 추적 + 파워 아이템 회수, 무피격)

히트박스를 키운 뒤로는 **두 가지 자동 플레이**로 나눠 측정합니다.
하나로는 "밸런스가 맞는가"와 "살아남을 수 있는가"를 동시에 볼 수 없기 때문입니다.

**① 추적 AI** — 타깃을 정확히 조준, 무적 강제. 구간 길이·타임아웃 측정용.

| 시나리오 | 전체 | STAGE | 논스펠 | 요격1 | 스펠1 | 요격2 | 스펠2 | 요격3 | 스펠3 | 진입 파워 | 타임아웃 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Normal | **3:07** | 58.9 | 13.9 | 16.6 | 18.9 | 22.2 | 18.9 | 21.4 | 15.9 | MAX | 0 |
| Hard | **3:39** | 66.3 | 22.2 | 16.5 | 22.6 | 22.2 | 24.6 | 21.4 | 23.1 | MAX | 0 |

**② 회피 AI** — 주변 탄에서 밀려나며 실제로 회피, **무적 없음**. 생존 가능성 측정용.

| 시나리오 | 결과 | 전체 | 피격 | 남은 잔기 | 실드 방어 | 최대 동시 탄 |
|---|---|---|---|---|---|---|
| Normal | CLEAR | 3:23 | **0회** | 4 | 1 | 120 |
| Hard | CLEAR | 4:00 | 1회 (논스펠) | 3 | 0 | 127 |

- 커진 히트박스로도 **전 난이도 클리어 가능**하며, 피격은 파워가 낮은 STAGE 초반에 집중
- 최대 동시 탄 수 160 → **120** (히트박스 확대에 맞춘 밀도 하향의 결과)
- 실측 아이템 획득: 유도 3〜7개 / 봄 2〜3개 / 실드 3〜4개

### ★ 평균 동시 탄 수 (난이도 × 페이즈)

| | Phase 0 | Phase 1 | Phase 2 | Phase 3 |
|---|---|---|---|---|
| Easy   | 18 | 72 | 17 | 40 |
| Normal | 23 | 84 | 21 | 49 |
| Hard   | 29 | 94 | 25 | 58 |

Phase 1(회전 링)이 물량 최대, Phase 2(십자포화)는 밀도보다 **이동 강제**가 목적이라
의도적으로 낮습니다. 난이도별 단조 증가가 전 페이즈에서 성립합니다.

---

## 15. 프로토타입 제외 (차기 과제)

- ~~잡몹 웨이브 / 스테이지 진행~~ → **구현 완료 (§4-B)**
- ~~파워업 아이템~~ → **구현 완료 (§4-A)**
- 자기 캐릭터 선택
- 리플레이 저장, 랭킹
- BGM
- STAGE 2 이상, 보스 2체 이상, 추가 스펠카드
- 로컬 저장소 기반 최고 점수 영속화

---

## 16. 구현하며 발견한 것 / 다음에 볼 것

**해결한 문제**

1. **전 페이즈 타임아웃** — 초안 HP 1200으로는 플레이어 화력이 전혀 못 미쳐 4페이즈 모두
   40초 제한시간으로 끝났습니다. HP 700, 보스 피격 반경 40px, 자탄 속도 13→16으로 조정하고
   보스 좌우 이동 폭을 줄여 해결했습니다.
2. **Phase 2·3 밀도 부족** — Phase 3(최종장)이 Phase 1보다 한산한 역전 현상이 있었습니다.
   간격 압축 + Phase 2에 낙하벽 추가로 바로잡았습니다.
3. **캡슐 스프라이트 십자 아티팩트** — 자탄/레이저의 글로우가 가로 방향으로 감쇠하지 않아
   `+` 모양으로 뭉개졌습니다. 폭 방향 감쇠 레이어 방식으로 다시 구웠습니다.
4. **오버킬 점수** — 남은 HP를 넘는 데미지도 점수에 반영되던 것을 실제 적용량만 반영하도록 수정.

**스테이지 구간 추가하며 해결한 문제**

5. **파워가 무의미했음** — 옵션 포드가 바깥으로 벌어져 파워 67% 상승이 보스 클리어 9% 단축에
   그쳤습니다. 포드를 안쪽으로 수렴시켜 68초 ↔ 103초로 벌렸습니다. (§4-A)
6. **파워가 너무 빨리 만렙** — 초안(개당 0.10)에서는 WAVE 3에서 이미 MAX에 도달해
   중간보스가 3초 만에 녹았습니다. 개당 0.06 + 중간보스 HP 800으로 재조정.
7. **페이즈 소요시간이 10〜26초로 들쭉날쭉** — 페이즈별 HP 보정으로 15.7〜18.3초에 수렴. (§6.2)
8. **무파밍 시 타임아웃** — 파워 Lv.1로 보스에 도달하면 Phase 2가 40.8초로 제한시간에 걸렸습니다.
   Lv.1/Lv.2의 발사 간격을 5→4프레임으로 완화해 해결 (전 시나리오 타임아웃 0회).

**파워 강화 / 획득 완화 요청을 반영하며 (2차 조정)**

9. **자석 반경을 130까지 키웠더니 파밍이 무의미해졌습니다** — 파밍 플레이와 방치 플레이가
   **둘 다 MAX에 도달**해(둘 다 보스 55.5초) 이전에 만들어 둔 설계 축이 무너졌습니다.
   105로 되돌리고 드랍 개수를 늘리는 쪽으로 방향을 바꿔, 획득은 쉬워지되
   **파밍 = MAX / 방치 = Lv.3** 이라는 차이는 남도록 맞췄습니다.
10. 화력을 1.5〜1.8배 올린 만큼 보스 HP를 700 → 1,080으로 함께 올렸습니다.
    다만 **전부 상쇄하지는 않아서** 보스 구간은 68초 → 53초로 실제로 짧아졌습니다.

**기체 축소 / 파워 강등 / 잡몹 체력 (5차)**

18. **자기 기체와 적기의 축소 배율을 분리했습니다** (`PLAYER_SCALE 0.55` / `PLANE_SCALE 0.7`).
    하나의 상수로 묶어두면 자기 기체만 줄일 때 적 히트박스까지 같이 작아져
    처치 시간이 또 늘어납니다 (4차에서 겪은 문제).
19. **피격 = 파워 한 단계 강등**으로 규칙을 명확히 했습니다. 기존 "1.0 감소"는 시작 위치에
    따라 레벨이 안 내려가는 경우가 있었습니다. 자기 히트박스가 8.25px로 작아진 만큼
    페널티를 강화해 균형을 맞췄습니다.
20. **잡몹 체력을 절반 가까이 낮췄습니다** (4/7/13). 요격 구간이 3개 늘어난 상태에서
    같은 체력은 반복 노동처럼 느껴집니다.

**비주얼 개선 — 블룸·뱅킹·이펙트 (6차)**

21. **블룸 구현 방식을 두 번 갈아엎었습니다.** `ctx.filter='blur()'`는 60→21 FPS,
    메인 캔버스 축소 복사는 60→30 FPS로 둘 다 실패했습니다. 발광 오브젝트만
    작은 캔버스에 재렌더하는 방식이 비용이 거의 없으면서 그림도 더 깨끗했습니다.
22. **느려진 진짜 원인은 블룸이 아니라 파티클이었습니다.** 처음엔 블룸을 의심해 계속
    손봤는데, 조건을 나눠 재보니 블룸은 1 FPS 수준이고 파티클이 23 FPS를 먹고 있었습니다.
    추측 대신 변수를 분리해 측정한 뒤에야 제대로 고칠 수 있었습니다.
23. **판정 원이 뱅킹을 따라 기울면 안 됩니다.** 처음엔 기체와 함께 기울였는데,
    판정이 타원으로 보여 플레이어를 오도합니다. 뱅킹 변환 밖으로 뺐습니다.

**온라인 2인 협동 (7차)**

24. **"외부 에셋 0개" 원칙이 이 기능에서 깨집니다.** 시그널링 없이는 두 브라우저를
    이을 방법이 없습니다. 대신 전송 계층을 `Transport` 인터페이스로 분리해,
    나중에 자체 WebSocket 서버로 갈아끼울 수 있게 했습니다.
25. **P2 기체를 `source-atop` 합성으로 틴트했더니 화면에 주황 사각형이 칠해졌습니다.**
    `alpha:false` 컨텍스트에서는 캔버스 전체가 불투명이라 합성 대상이 스프라이트가
    아니라 화면 전체가 됩니다. P2 전용 스프라이트를 따로 굽는 것으로 해결했습니다.
26. **공개 브로커에 의존하면 자동 테스트를 할 수 없어서**, BroadcastChannel 기반
    전송을 하나 더 만들었습니다. 로컬 HTTP 서버 + 탭 2개로 대기실·스타트·동기화·부활을
    전부 자동 검증합니다.

**히트박스 정합 / 기체 조정 (4차)**

15. **적 히트박스를 스프라이트에 맞추자 처치 시간이 급증했습니다** — 기체를 70%로 줄이면서
    적 판정 반경도 30% 작아져, STAGE가 70초 → 104초가 되고 Normal 논스펠이 40.8초로
    제한시간에 걸렸습니다. 적 HP를 30% 낮춰(10/16/30 → 7/11/21) 되돌렸습니다.
16. **자탄·포드·애프터버너 발사 위치가 기체와 어긋났습니다** — 스프라이트만 0.7배로 줄이고
    발사 오프셋은 그대로여서 포드가 기체에서 떨어져 보였습니다. 전부 `PLANE_SCALE`을 타도록 수정.
17. **회피 AI로 측정하니 Hard 논스펠이 40초 제한에 걸렸습니다** — 회피에 집중하면 조준이
    흐트러지는 실제 상황입니다. 제한시간을 50초로 늘려 "잘 피했는데 보너스를 못 받는" 상황을
    없앴습니다.

**항공기 테마 전환 / 요격 구간 편성 (3차)**

11. **미사일이 별 모양으로 보였습니다** — 핀을 몸통 폭의 3배로 그린 탓. 0.6배로 줄이고
    전장을 폭의 3.5〜4배로 늘려서야 미사일로 읽혔습니다. (§10)
12. **요격 구간이 6〜10초밖에 안 됐습니다** — 편대 스폰 간격(12프레임)만 계산하고
    편대 사이 대기 시간을 넣지 않아서, 스크립트 자체가 5.5초짜리였습니다.
    대기를 넣어 16〜22초로 맞췄습니다.
13. **아이템이 미사일과 헷갈리는 문제** — 색만 바꾸는 걸로는 밀도가 높을 때 다시 섞입니다.
    형태(각진 배지)·색 계열(탄이 안 쓰는 초록/하늘/보라)·움직임(흔들림) 세 축을
    동시에 분리했습니다.
14. **HUD가 하단 바 밖으로 잘렸습니다** — HOMING 라벨이 y=636에 그려져 640px 캔버스
    아래로 넘어갔습니다. 배치를 다시 잡았습니다.

**남은 관찰 / 판단 필요**

1. **세로로 긴 폰에서 레터박스가 큼** — 논리 해상도 480×640 고정이라 19.5:9 화면에서는
   위아래로 검은 띠가 넓게 남습니다. 기획서상 의도된 동작이지만 모바일 체감은 아쉽습니다.
   플레이필드 세로 길이를 화면 비율에 맞춰 늘리면 해결되나, 회피 공간이 넓어져
   **난이도가 함께 바뀌므로** 임의로 바꾸지 않고 남겨뒀습니다.
2. **익스텐드 기준점 100만점** — Normal 클리어 점수가 약 102만이라 대부분의 클리어에서
   막판에 잔기 +1이 들어옵니다. 의도한 보상 타이밍인지 확인 필요.
3. **Phase 2 체감** — 밀도가 낮은 대신 조준탄 압박이 핵심입니다. 실제로 플레이해 보고
   "심심하다"고 느껴지면 낙하벽 빈도를 올리는 것이 가장 손쉬운 손잡이입니다.
4. **페이즈별 HP 보정값은 제 자동 플레이 기준으로 역산한 값입니다.** 사람의 조준 습관이
   다르면(예: 보스 바로 밑에 붙는 플레이) Phase 1이 다시 짧아질 수 있습니다.
   실제로 몇 판 해보시고 특정 페이즈가 유독 길거나 짧으면 `PHASE_HP_SCALE` 배열만
   만지면 됩니다.
5. **파밍 vs 사격의 트레이드오프가 의도치 않게 잘 나왔습니다** — 파밍하러 움직이면
   편대 전멸을 놓치고(6회), 사격에 집중하면 파워가 안 오릅니다(전멸 8회 / 파워 Lv.3).
   두 스코어링 축이 서로 경쟁하는 구조라 그대로 두는 편이 좋아 보입니다.
6. **파밍/무파밍 차이가 다시 줄었습니다 (3:19 vs 3:20).** 요격 구간이 3개 늘면서
   아이템 총량이 약 160개로 늘어, 파밍을 신경 쓰지 않아도 Lv.2〜3까지는 자연히 오릅니다.
   차이는 여전히 첫 스펠카드에서 드러나지만(18.4초 vs 24.3초), 지난번만큼 뚜렷하진 않습니다.
   **요격 구간 추가와 파밍 차별화는 서로 상충하는 목표**라, 지금은 "요격 구간이 파워 회복
   기회를 준다"는 쪽에 무게를 두었습니다. 차이를 다시 벌리려면 요격 구간의 파워 드랍만
   줄이면 됩니다.
7. **더 쉽게 만들 여지** — 동방의 POC 라인(화면 상단으로 올라가면 화면 내 아이템 전량 자동 회수)을
   넣으면 획득이 한층 쉬워지면서도 "위험을 감수하고 위로 올라간다"는 판단 요소가 생깁니다.
   요청 범위를 넘는 새 기제라 넣지 않았습니다.
8. **고탄수 성능 여유가 줄었습니다** (1,000탄 60→34 FPS). 실사용 구간(160탄)은 60 FPS라
   지금은 문제없지만, 탄 수를 크게 늘릴 계획이라면 미사일 스프라이트 크기부터 줄이는 것이
   가장 효과적입니다. (§13)
10. **그레이즈 획득량이 크게 줄었습니다** (런당 약 200 → 약 90). 밀도를 낮추고 그레이즈 밴드
    폭이 상대적으로 좁아진 결과입니다. 그레이즈 점수 비중을 유지하려면 `grazeR`의 +20px 밴드를
    +28px 정도로 넓히는 것이 가장 간단합니다.
11. **탄 밀도(`DENS_SCALE = 0.75`)는 되돌릴 수 있는 손잡이입니다.** 0.45까지 낮췄을 때는
    최대 동시 탄이 75발까지 떨어져 "탄막게임" 느낌이 옅어졌고, 0.75에서 120발로 회복하면서도
    회피 AI가 클리어했습니다. 더 빡빡하게 원하시면 이 값 하나만 올리면 됩니다.
9. **보스/중간보스는 항공기로 만들지 않았습니다** — 선택하신 "미그 3종 + 보스" 안에 맞춘
   것이고, 추상 코어 형태가 '탄막 발사원'으로 더 잘 읽히기 때문입니다. 대형 폭격기 형태로
   바꾸길 원하시면 알려주세요.

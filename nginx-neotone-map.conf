# NEOTONE — WebSocket 업그레이드용 map (http 컨텍스트)
# conf.d/*.conf 는 nginx.conf 의 http{} 안에서 include 되므로 여기에 두면 된다.
# 기존 server 블록에는 영향을 주지 않는다.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
